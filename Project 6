#File Transfer App
mport shutil
import os
source = input("Enter the path of the file to transfer: ")
if not os.path.isfile(source):
    print("File does not exist!")
else:
    DEEPU = input("Enter the DEEPU folder path: ")
    if not os.path.isdir(NEHA):
        print("DEEPU folder does not exist!")
    else:
        try:
            shutil.copy (source, DEEPU)
            print("File transferred successfully!")
        except Exception as e: 
            print("Error transferring file:", e)
